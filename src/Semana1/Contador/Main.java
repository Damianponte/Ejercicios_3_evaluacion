package Semana1.Contador;

public class Main {
    public static void main(String[] args) {
        //Ejercicio 1:
        //Crea una clase Semana1.Contador con:
        //- un atributo static int contador
        //- un método static void incrementar() que aumente el contador en 1
        //- un método static int getContador()
        //Desde una clase Principal1, llama varias veces a incrementar() y muestra el valor final.
        // Recuerda que es todo static,
        // NO tienes que crear una variable de tipo Semana1.Contador, sino usar Semana1.Contador.incrementar()



       Contador.incrementar();
       Contador.getContador();
        Contador.incrementar();
        Contador.getContador();

        Contador.incrementar();
        Contador.getContador();

        Contador.incrementar();
        Contador.getContador();

        Contador.incrementar();
        Contador.getContador();

        Contador.incrementar();
        Contador.getContador();





    }
}
